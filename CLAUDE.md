# CLAUDE.md — Lobster SC OS (Ernawa)

Aturan berikut bersifat MENGIKAT untuk seluruh pekerjaan di repo ini. Jangan menyimpang dari aturan ini kecuali pengguna memberi instruksi eksplisit untuk kasus tertentu.

## 1. Pemisahan Dua Track Bisnis (Trading vs Budidaya)

Proyek ini punya DUA TRACK bisnis yang wajib dipisah total:

- **Trading** — Ernawa sebagai pengepul, modal lincah (cepat berputar).
- **Budidaya** — land-based di Cimahi, modal terkunci dalam biomassa.

Aturan:
- JANGAN PERNAH membuat unit economics gabungan (blended) antara dua track ini — di kode, query, maupun laporan mana pun.
- Setiap tabel dan kalkulasi finansial harus punya kolom/filter `track` yang eksplisit (mis. `track = 'trading' | 'budidaya'`).
- Setiap agregasi (revenue, margin, cash flow, dsb.) harus di-`GROUP BY`/difilter per track sebelum ditampilkan atau dijumlahkan lintas track. Jika sebuah laporan benar-benar butuh angka gabungan (mis. total kas perusahaan), tampilkan breakdown per track di sampingnya — jangan hanya angka blended.

## 2. Inventory sebagai Ledger Append-Only

Stok/inventory TIDAK BOLEH disimpan sebagai angka yang di-mutate langsung (tidak ada `UPDATE quantity` in place).

- Gunakan pola **Inventory Ledger append-only**: setiap pergerakan stok adalah baris baru.
- `movement_type` yang valid: `receive`, `mortality`, `shrinkage`, `delivery`, `reject`, `transfer_in`, `transfer_out`, `adjustment`.
- Stok yang tersedia SELALU dihitung sebagai `SUM()` dari ledger pada saat query — bukan dibaca dari kolom running-total yang tersimpan.

## 3. Koreksi Data via Reversal, Bukan UPDATE/DELETE

- Baris ledger yang sudah tersimpan TIDAK BOLEH di-`UPDATE` atau di-`DELETE`.
- Koreksi harus berupa baris **reversal** baru yang mereferensikan baris asal lewat kolom `reversal_of` (menunjuk ke id baris yang dikoreksi).

## 4. Row Level Security (RLS) Wajib Aktif

- RLS WAJIB aktif di semua tabel Postgres/Supabase sejak schema pertama dibuat.
- Jangan pernah menyarankan menonaktifkan RLS "untuk sementara" atau "biar gampang development" — termasuk saat debugging atau prototyping cepat.

## 5. Pemisahan Kunci Supabase

- `service_role` key Supabase TIDAK BOLEH pernah muncul di kode frontend/client-side, dan TIDAK BOLEH pernah di-commit ke git (termasuk di `.env` yang ter-tracked, config, atau contoh kode).
- `service_role` key hanya boleh dipakai di server-side / edge function.
- Kunci yang boleh ada di frontend hanyalah **anon public key**.

## 6. Bahasa dan Format Angka di UI

- Semua tampilan yang menghadap pengguna (user-facing) menggunakan **Bahasa Indonesia**.
- Format angka menggunakan format Indonesia: titik (`.`) sebagai pemisah ribuan, koma (`,`) sebagai pemisah desimal.
- Contoh format mata uang: `Rp520.000`.

## 7. Perubahan Skema via Migration File

- Setiap perubahan skema database harus berupa file migration bernomor urut di folder `migrations/`.
- Jangan pernah mengubah skema langsung lewat dashboard Supabase tanpa migration file yang tersimpan di git.

## 8. Konfirmasi Sebelum Migration Destruktif

- Sebelum menjalankan migration apa pun yang mengandung `DROP` atau perintah destruktif lain (termasuk `TRUNCATE`, `DELETE` massal, `ALTER ... DROP COLUMN`), tampilkan dulu isi lengkap migration tersebut ke pengguna.
- Minta konfirmasi eksplisit dari pengguna sebelum eksekusi — jangan pernah langsung menjalankannya.

---

## Catatan Tech Debt

Bagian ini BUKAN aturan mengikat seperti nomor 1-8 di atas — ini gap yang diketahui dan SENGAJA ditunda, dicatat di sini supaya tidak lupa dan tidak diselesaikan setengah-setengah (mis. dibetulkan di satu halaman tapi lupa di halaman lain yang punya gap identik).

- **[SELESAI — migration 0010]** ~~Non-atomicity di alur multi-insert dari client (Terima Cepat & Alokasi/Kirim)~~: `Inventory > Terima Cepat` (insert `receiving_transactions` → `receiving_lots` → RPC `get_or_create_batch()` → insert `batch_lines`) dan `Deliver > Alokasi & Kirim` (insert `deliveries` → insert `delivery_allocations`) sama-sama dieksekusi sebagai beberapa panggilan Supabase berurutan dari client, BUKAN satu transaksi database yang atomik. Kalau gagal di tengah jalan, bisa tersisa data parsial/"nyangkut" (mis. `deliveries` tanpa `delivery_allocations`, tidak bisa di-rollback karena kedua tabel tidak punya policy DELETE). Ini GAP YANG SAMA di kedua alur, bukan dua masalah terpisah — begitu volume transaksi bertambah, keduanya WAJIB dibungkus jadi RPC/stored procedure atomik dalam SATU migration, bukan dibetulkan satu-satu per halaman secara terpisah.
  **Resolusi**: migration 0010 menambahkan `create_receiving_with_batch()` dan `create_delivery_with_allocations()` (keduanya SECURITY INVOKER, 1 transaksi all-or-nothing masing-masing). `TerimaCepatPage.tsx` dan `AlokasiKirimPage.tsx` diupdate untuk memanggil RPC ini, pola retry/orphan-delivery yang lama sudah dihapus dari `AlokasiKirimPage.tsx`.
- **[SELESAI — migration 0010]** ~~Validasi qty vs saldo batch_line di Alokasi & Kirim cuma client-side, belum ada DB-level guard terhadap race condition~~: `AlokasiKirimPage` mengecek `qty <= balance_kg` di JavaScript sebelum submit (pakai saldo yang di-fetch lewat `get_available_batch_lines` beberapa saat sebelumnya), tapi tidak ada CHECK/trigger di database yang re-verifikasi saldo pada saat `INSERT INTO delivery_allocations` benar-benar terjadi. Kalau 2 user mengalokasikan dari `batch_line_id` yang sama secara bersamaan (concurrent), keduanya bisa lolos validasi client (karena masing-masing membaca saldo "lama" yang sama-sama masih terlihat cukup), dan hasil akhirnya stok bisa over-allocated (`inventory_ledger` boleh saja jadi negatif — tidak ada CHECK yang melarang saldo negatif). WAJIB ditangani (constraint atau trigger yang re-check `SUM(inventory_ledger.qty_kg)` sisa terhadap `qty_kg` yang mau diinsert, di level DB) sebelum ada lebih dari satu user aktif input alokasi secara bersamaan.
  **Resolusi**: `create_delivery_with_allocations()` (migration 0010) mengunci baris `batch_lines` terkait (`SELECT ... FOR UPDATE OF bl`, diurutkan by `batch_line_id` untuk cegah deadlock) sebelum menghitung ulang `SUM(inventory_ledger.qty_kg)` dan membandingkannya dengan qty yang diminta — dalam transaksi yang sama dengan insert `deliveries`/`delivery_allocations`, jadi race condition maupun kegagalan validasi sama-sama rollback total.
  **KOREKSI (migration 0013)**: resolusi di atas ternyata KELIRU untuk role lapangan. `SELECT ... FOR UPDATE` di tabel ber-RLS juga mensyaratkan lolos policy UPDATE, dan `batch_lines_update` owner-only — jadi untuk `lead_lapangan`/`staf_lapangan` baris tersaring diam-diam dan RPC melempar "batch_line_id tidak ditemukan" (hanya owner yang bisa alokasi). Terverifikasi lewat simulasi role di DB produksi. Migration 0013 mengganti row-lock dengan `pg_advisory_xact_lock` per batch_line (tetap SECURITY INVOKER, tanpa policy UPDATE baru). PELAJARAN: setiap klaim "RLS aman" untuk sebuah function WAJIB diuji dengan simulasi SETIAP role yang memanggilnya (set local role authenticated + request.jwt.claims), bukan cuma owner.
- **[SELESAI — migration 0016/0017]** ~~`users` belum punya site/track scoping (KETERBATASAN SENGAJA untuk fase pilot, BUKAN bug)~~: tabel `users` cuma punya kolom `id`, `full_name`, `role` — tidak ada `site_id` atau `track`. Konsekuensinya, SEMUA `lead_lapangan`/`staf_lapangan` otomatis bisa akses SEMUA site (trading maupun budidaya) lewat RLS yang ada sekarang — tidak ada mekanisme "staf ini cuma boleh site A" atau "staf ini cuma boleh track trading/budidaya". Ini keputusan sadar untuk tim pilot yang masih kecil dan saling percaya (sudah dicatat sejak komentar migration 0003, "Belum ada pembatasan per site pada fase pilot ini"). WAJIB direvisit (tambah kolom scoping di `users` + policy RLS yang memfilter berdasarkan itu, di semua tabel operasional yang relevan) SEBELUM: (a) tim bertambah di luar circle awal, atau (b) ada kebutuhan pemisahan akses per-track yang lebih ketat (mis. staf budidaya tidak boleh melihat data trading sama sekali) — jangan onboarding staf baru dengan asumsi scoping ini sudah ada, karena belum.
  **Resolusi**: migration 0016 menambah tabel `user_sites` (penugasan user ke site; track mengikuti `sites.type`) dan helper `user_can_access_site()` (SECURITY DEFINER, owner selalu true). Migration 0017 mengganti (ALTER POLICY) policy SELECT/INSERT di semua tabel operasional yang terikat site supaya lead/staf hanya bisa site yang ditugaskan; user lapangan tanpa penugasan tidak melihat apa pun (gagal-tertutup). Diuji dengan simulasi role (hanya-Sorong, hanya-Banggai, lintas-site) sebelum diterapkan. Halaman Manajemen User mengatur penugasan. SISA (sengaja): `demands`, `customers`, `suppliers`, `products` tetap global karena tidak punya `site_id`; pesan error mortalitas di site tak-ditugaskan berbunyi "saldo tidak cukup" (trigger guard jalan sebelum RLS) — tetap ditolak, hanya pesannya kurang tepat.
- **[SELESAI — migration 0018]** ~~Trigger `fn_users_protect_role` (migration 0012) belum menganggap koneksi `service_role` sebagai trusted~~ (dugaan di bawah terbukti benar saat Edge Function `invite-user` dibangun; diperbaiki dengan mengecek klaim `role = 'service_role'` di `request.jwt.claims`, diuji: service_role lolos, lead tetap ditolak, owner lolos, klaim palsu ditolak): fix di 0012 membedakan "ada konteks request JWT" (`current_setting('request.jwt.claims', true) is not null`) vs "koneksi DB langsung tanpa JWT" (SQL Editor). Tapi panggilan lewat REST API pakai `service_role` key (mis. dari job otomatis/backend service di masa depan) **tetap punya konteks JWT** — `service_role` key itu sendiri adalah JWT dengan claim `role: "service_role"`, jadi `v_has_request_context` tetap `TRUE` untuk itu, BUKAN `FALSE` seperti koneksi SQL Editor. Konsekuensinya: kalau nanti ada backend service/cron job yang connect lewat REST API pakai `service_role` key (bukan raw SQL) dan mencoba insert/update `users.role`, trigger ini kemungkinan BESAR akan tetap menolaknya (karena `auth_user_role()` juga tidak akan bernilai `'owner'` untuk request service_role yang tidak terasosiasi user manapun) — padahal `service_role` seharusnya bisa bypass pembatasan semacam ini. WAJIB ditest eksplisit begitu backend service pertama yang pakai `service_role` key dibangun dan perlu menyentuh tabel `users`. Kalau ternyata bermasalah, kemungkinan perlu deteksi tambahan lewat `auth.role() = 'service_role'` (bukan cuma cek ada/tidaknya request context saja).
- **[SELESAI — Edge Function `invite-user`, migration 0018]** ~~Manajemen User belum bisa membuat akun baru~~ (Owner kini membuat akun dari aplikasi lewat Edge Function server-side; katasandi sementara ditampilkan sekali, user menggantinya lewat Home > Ganti Katasandi. SISA: tidak ada pengiriman email — katasandi sementara disampaikan Owner secara manual; source function ada di `supabase/functions/invite-user/`, deploy lewat Supabase, bukan lewat Cloudflare): halaman `Admin > Manajemen User` hanya mengubah role user yang SUDAH punya akun. Membuat akun (auth.users) butuh Supabase Admin API / `service_role` key yang TIDAK BOLEH ada di frontend (aturan #5). Saat ini akun baru dibuat manual lewat dashboard Supabase Auth + insert ke `public.users`. Undangan user dari UI butuh Edge Function server-side (sekaligus menjadi kasus uji pertama untuk catatan `fn_users_protect_role` + `service_role` di atas).
- **[SELESAI — migration 0024]** ~~`quality_inspections.grade` teks bebas~~: skala grade ditetapkan A/B/C, ditegakkan lewat CHECK constraint (`quality_inspections_grade_check`) di DB, form Inspeksi Kualitas pakai dropdown (bukan teks bebas). Catatan ini sempat tertinggal belum ditandai selesai walau migration-nya sudah lama diterapkan — dikonfirmasi ulang lewat query constraint langsung ke produksi sebelum ditandai.
- **[SELESAI — migration 0019/0020/0021/0022]** Empat butir PRD P1 yang sebelumnya terbuka: (B3) status demand `partial` — total teralokasi dihitung lewat `demand_allocated_kg()` SECURITY DEFINER karena demand global sedangkan alokasi ter-scope per site; (B2) `cancel_delivery()` owner-only membalik ledger alokasi lewat reversal, ditolak bila sudah ada settlement, view keuangan mengecualikan delivery batal; (B6) tabel `stock_adjustments` (penyusutan/reject, alasan wajib, guard saldo, koreksi lewat Koreksi Ledger); (B4) role `investor` hanya membaca data Finance lewat 4 fungsi `investor_*` SECURITY DEFINER (tanpa SELECT ke tabel mentah, tanpa hak tulis). Semuanya diuji dengan simulasi role di transaksi yang di-rollback sebelum diterapkan.
- **Sisa dari advisor Supabase**: (c) "Leaked password protection" belum aktif di Supabase Auth (setelan dashboard) — SENGAJA ditunda sampai akhir atas instruksi eksplisit pengguna, relevan karena akun baru memakai katasandi sementara. (d) `rls_auto_enable()` bukan buatan migration proyek ini — periksa asalnya sebelum mengubahnya. Poin (a) EXECUTE trigger functions dan (b) search_path INVOKER functions sudah SELESAI (lihat catatan migration 0023 & 0030 di bawah) — SISA yang terekspos ke authenticated (`auth_user_role`, `demand_allocated_kg`, `investor_*`, `user_can_access_site`) memang harus bisa dipanggil user login, bukan gap.
- **[SELESAI — migration 0023]** Poin (a) dan (b) dari catatan advisor di atas: `EXECUTE` 14 fungsi trigger dicabut dari public/anon/authenticated (trigger tetap menyala — Postgres hanya memeriksa EXECUTE saat CREATE TRIGGER), dan `search_path = public` dikunci pada 12 fungsi INVOKER. Diuji dengan menjalankan seluruh jalur tulis yang melewati trigger sebagai lead & owner sebelum diterapkan. SISA advisor yang disengaja/di luar migration: `auth_user_role()`, `user_can_access_site()`, `demand_allocated_kg()`, `investor_*()` memang harus bisa dipanggil user login; `rls_auto_enable()` bukan buatan proyek ini; "Leaked password protection" adalah setelan dashboard Supabase Auth (belum diaktifkan).
- **[SELESAI — PRD C4]** Rapikan teknis: (a) client Supabase kini bertipe penuh (`createClient<Database>` + `src/types/database.ts` hasil `generate_typescript_types`) — **setiap kali menambah migration, generate ulang `database.ts`** supaya build menangkap query yang tidak cocok dengan skema; (b) halaman dimuat per-route lewat `React.lazy` (bundle awal ±429 kB, tanpa peringatan ukuran chunk); (c) `.vscode/` di-ignore dan `.gitattributes` memaksa LF (tidak ada lagi peringatan LF/CRLF); (d) customer uji `asdsad`/`aaaaaa` dinonaktifkan lewat status (riwayat utuh).
- **[SELESAI]** Empat temuan dari review PRD eksternal (21 Sept): (1) migration 0026 — override FEFO ditegakkan di server (`create_delivery_with_allocations`), plus perbaikan bug lama (deteksi lama membanding lintas produk berbeda, seharusnya per produk); (2) migration 0027 — Kas Panjar (`cash_ledger` dapat `pic_user_id`, saldo per PIC-per-site, guard saldo negatif, `create_cash_reversal`, halaman Finance > Kas Panjar); "Finance" pada PRD sumber dipetakan ke role `owner` yang sudah ada (bukan role baru); (3) migration 0028 — `ref_price()` fallback VWAP (override manual -> VWAP7 site -> VWAP7 semua site -> VWAP30 semua site -> NULL), dihitung dari `receiving_lots` (harga BELI, bukan jual — satu delivery bisa multi-produk sejak 0026 sehingga tidak ada cara akurat memecah nilai jual per produk); halaman Source > Acuan Harga. **BELUM dibangun dari poin ini**: FR-PRICE-002 (warning spread minimum thd acuan jual) — butuh referensi harga JUAL per produk yang akurat, yang skema ini belum bisa hitung dengan benar; (4) Import Excel untuk Terima Cepat (`src/lib/importExcel.ts` + halaman Inventory > Import Terima Cepat), pakai `exceljs` (BUKAN `xlsx`/SheetJS dari npm — high severity, tanpa perbaikan). Sisa audit: `exceljs` moderate lewat `uuid` transitif (butuh param `buf` eksplisit yang tidak dipakai exceljs secara internal — risiko rendah, dipantau, upgrade kalau ada versi yang memperbaikinya).
- **[SELESAI — migration 0029]** ~~Serah Terima (Handover) antar site tidak pernah benar-benar berfungsi sejak migration 0001~~: trigger `fn_handover_lines_ledger` lama menulis `transfer_out` DAN `transfer_in` ke `batch_line_id` yang SAMA (bug net-nol) — tidak ada batch_line baru dibentuk di site tujuan, dan tidak ada UI sama sekali di atasnya. Root cause: `batch_lines.receiving_lot_id` NOT NULL, seluruh rantai `batch_lines -> receiving_lots -> receiving_transactions` mengasumsikan stok selalu dari pembelian supplier, tidak ada jalur untuk batch_line asal transfer internal.
  **Resolusi**: didesain ulang jadi dua langkah/dua aktor (dikonfirmasi user: qty diterima boleh beda dari qty dikirim — selisih otomatis jadi `shrinkage`; tank tujuan dipilih penerima saat konfirmasi, bukan saat kirim). (1) **Kirim** — `create_handover_dispatch()` (SECURITY INVOKER, cukup akses site asal): validasi saldo per batch_line (advisory lock, pola sama seperti alokasi delivery), insert `handovers`+`handover_lines`, trigger `fn_handover_lines_ledger` (diperbaiki) langsung mencatat `transfer_out` di batch_line asal — stok berkurang seketika, tidak bisa dialokasikan lagi selama "di jalan". (2) **Konfirmasi Terima** — `confirm_handover_receipt()` (SECURITY INVOKER, cukup akses site tujuan): bikin `receiving_transaction` sintetis (`supplier_id` NULL, `source_handover_id` terisi, harga pokok diwariskan dari batch asal untuk kontinuitas margin) + `receiving_lot` + `batch_line` BARU di site tujuan lewat `get_or_create_batch()`; insert batch_line ini memicu `fn_batch_lines_ledger` (diperbaiki: pakai movement_type `transfer_in` kalau asalnya handover, `receive` kalau bukan). Kalau qty diterima < qty dikirim, trigger baru `fn_handover_lines_confirm_variance` (SECURITY DEFINER — perlu ini karena `inventory_ledger` INSERT policy owner-only, sama seperti kenapa ledger normal juga selalu lewat trigger bukan RPC langsung) membalik `transfer_out` awal via `reversal_of` lalu menggantinya dengan `transfer_out` sejumlah qty diterima + `shrinkage` sejumlah selisihnya, keduanya di batch_line asal. RLS `handovers_update`/`handover_lines_update` diperlonggar dari owner-only jadi juga mengizinkan non-owner dengan akses site tujuan (selama belum dikonfirmasi). `ref_price()` (VWAP) diperbarui mengecualikan `receiving_transactions` asal handover supaya stok pindah-tangan tidak terhitung sebagai sinyal harga pembelian baru. Halaman `Inventory > Serah Terima` (`HandoverPage.tsx`): form Kirim (pilih stok via `get_available_batch_lines`, sama seperti Alokasi & Kirim) + daftar "Menunggu Konfirmasi Terima" dengan form konfirmasi per baris. Diuji 9 skenario simulasi role (rollback): no-variance, variance/shrinkage, RLS tolak-konfirmasi-tanpa-akses-tujuan, RLS tolak-kirim-tanpa-akses-asal, lead dengan akses lengkap, saldo tidak cukup, double-confirm, pengecualian VWAP, regresi Terima Cepat normal (movement_type tetap `receive`). **BELUM ada**: mekanisme "batalkan kiriman" sebelum dikonfirmasi (kalau salah kirim, sementara perlu Koreksi Ledger manual oleh owner).
  **Diuji lewat browser sungguhan (Playwright, produksi) setelah rilis**: alur penuh Kirim (Banggai) -> Konfirmasi Terima (Sorong, sengaja qty diterima < dikirim untuk uji shrinkage) berhasil end-to-end, ledger hasil akhir diverifikasi lewat SQL cocok persis dengan desain (net 0 di batch_line asal, net qty diterima di batch_line tujuan). **Bug ditemukan & diperbaiki** dari uji ini (tidak mungkin ketahuan dari simulasi SQL karena butuh PostgREST sungguhan): query `select` di `HandoverPage.tsx` untuk daftar "Menunggu Konfirmasi Terima" meng-embed `batch_lines` dari `handover_lines` tanpa nama constraint FK eksplisit — sejak migration 0029 menambah kolom `handover_lines.to_batch_line_id` (FK kedua ke `batch_lines`), PostgREST tidak bisa lagi menebak FK mana yang dimaksud dan menolak query ("Could not embed because more than one relationship was found"). Fix: `batch_lines!handover_lines_batch_line_id_fkey(...)` eksplisit. Data uji dibersihkan lewat baris ledger koreksi (bukan hapus) setelah verifikasi.
  **[SELESAI]** Gap yang ketahuan saat uji ini: tidak ada halaman UI untuk mengelola `tanks` — site `Sorong`/`Banten`/`Ujung Genteng` di produksi TIDAK PUNYA tank sama sekali (cuma `Banggai`), jadi handover/penerimaan sungguhan ke site itu butuh insert manual lewat SQL. **Resolusi**: halaman `Inventory > Kelola Tank` (`KelolaTankPage.tsx`) — list semua tank (siapa saja dengan akses site bisa lihat, `tanks_select`), tambah tank baru dan ubah nama owner-only (`tanks_insert`/`tanks_update`, RLS yang sudah ada sejak awal, tidak perlu migration baru). Tidak ada aksi hapus (tidak ada policy DELETE maupun kolom status di `tanks`) — konsisten dengan pola Customer/Supplier Management. Diverifikasi lewat smoke-test REST API langsung (embed `site:sites(name)` aman, cuma satu FK, tidak kena masalah ambigu seperti kasus `handover_lines` di atas; insert+select dicoba nyata lalu dibersihkan).
- **[SELESAI]** Kas Panjar & Import Excel kini juga diuji tulis-data end-to-end lewat browser sungguhan (Playwright, produksi), melanjutkan sesi uji Handover di atas.
  **Bug ditemukan & diperbaiki di `KasPanjarPage.tsx`** (tidak mungkin ketahuan dari simulasi SQL — soal timing React, bukan soal DB/RLS): `loadAll()` (fetch daftar PIC utk dropdown owner) dan default `formCategory` ('topup' utk owner) SAMA-SAMA dihitung dari `isOwner` yang dibaca SAAT MOUNT/render pertama, di `useEffect(..., [])` atau `useState(initializer)` — keduanya cuma jalan SEKALI. `authContext` fetch `profile` (sumber `role`/`isOwner`) secara ASINKRON terpisah dari `session`, dan `RequireAuth` merender halaman begitu `session` siap TANPA menunggu `profile` selesai. Kalau halaman ini diakses lewat full page load (refresh, buka link langsung, atau `page.goto` di test) alih-alih navigasi client-side dari halaman lain (yang kebetulan sudah meng-cache `profile` duluan), `isOwner` masih `false` saat kode itu jalan — dropdown "Pilih PIC" permanen kosong dan "Jenis" default diam-diam jadi "Pengeluaran" padahal login sebagai owner, walau tampilan lain (judul "Saldo Semua PIC", dst.) sudah benar owner. Fix: tunggu `profileLoading` (dari `useAuth()`) selesai dulu sebelum `loadAll()`/`setFormCategory` jalan.
  Setelah fix: diuji nyata — owner top-up Rp50.000 ke PIC "Field Admin" @ Banggai (berhasil, saldo tampil benar) -> Koreksi/reversal lewat tombol UI (saldo balik ke Rp0, riwayat menampilkan baris "Reversal" + tag "Dikoreksi" pada baris asal) -> guard saldo negatif DB terkonfirmasi tetap menolak percobaan pengeluaran melebihi saldo (pesan error tampil apa adanya ke user, bukan silent fail). Import Excel: file `.xlsx` nyata (1 baris valid + 1 baris sengaja invalid/supplier tidak ada) berhasil diupload & diparse benar (badge status tepat: "Tidak valid" dgn alasan jelas), baris valid diimpor sungguhan lewat `create_receiving_with_batch`, lalu file yang SAMA diupload ulang dan baris itu benar terdeteksi "Duplikat (dilewati)" (fitur idempotensi bekerja). Data uji dibersihkan lewat `create_ledger_reversal`/reversal Kas Panjar (bukan dihapus).
- **[SELESAI — migration 0030]** Sisa poin (b) dari catatan advisor Supabase: `create_delivery_with_allocations` (0026), `fn_cash_ledger_balance_guard`+`create_cash_reversal` (0027), `ref_price` (0028/0029) dibuat/diubah SETELAH migration 0023 mengunci 12 fungsi INVOKER lain — jadi luput dari hardening massal saat itu dan tetap muncul di `get_advisors` sebagai "Function Search Path Mutable". Ditutup dengan `CREATE OR REPLACE` menambah `SET search_path TO 'public'`, badan fungsi TIDAK diubah sama sekali. Diuji: `ref_price()` dipanggil langsung, `fn_cash_ledger_balance_guard`+`create_cash_reversal` diuji lewat insert+reversal kas simulasi role (rollback), `create_delivery_with_allocations` diuji lewat receive+delivery simulasi role (rollback) — semua jalan normal. `get_advisors` dicek ulang setelahnya: lint `function_search_path_mutable` sudah tidak muncul lagi.
- **[SELESAI]** Audit per-modul (22 Sept): `products` (jenis lobster) cuma bisa dibaca lewat dropdown di semua halaman — tidak ada UI untuk menambah jenis baru, gap yang sama seperti Kelola Tank. **Resolusi**: halaman `Source > Kelola Produk` (`KelolaProdukPage.tsx`), pola identik dengan `KelolaTankPage.tsx` (list + tambah + ubah nama, owner-only, RLS `products_insert`/`products_update` yang sudah ada sejak awal, tidak ada aksi hapus). Diverifikasi lewat smoke-test REST API langsung (select + insert nyata, dibersihkan). **Sisa gap dari audit yang sama**:
  - **[SELESAI — migration 0031]** (a) ~~batalkan kiriman Handover sebelum dikonfirmasi~~: `cancel_handover_dispatch()` (owner-only) membalik transfer_out awal via reversal, menandai `handovers.cancelled_at`/`cancelled_reason`. `confirm_handover_receipt()` sekalian diperbaiki menambah guard `cancelled_at is null` (gap baru yang ketahuan saat menulis migration ini — sebelumnya handover yang idealnya sudah batal masih bisa dikonfirmasi). Tombol "Batalkan" (owner-only) di `HandoverPage.tsx`. Diuji 5 skenario simulasi role (rollback).
  - **[SELESAI — migration 0032]** (c)+(f) ~~Laporan Risiko FEFO lintas-site~~ dan ~~Target/KPI config~~ ternyata satu akar masalah: `product_holding_policy` (ambang `max_holding_hours` per produk per track) KOSONG SAMA SEKALI di produksi dan tidak ada UI — fitur "lewat ambang holding" (sejak migration 0008) belum pernah benar-benar aktif di mana pun. `get_fefo_risk_report()` (SECURITY INVOKER, sama seperti `get_available_batch_lines` — RLS yang sudah ada otomatis membatasi per-site) mengembalikan SEMUA batch_line overdue lintas-site yang bisa diakses caller. Halaman `Inventory > Laporan Risiko FEFO` (`FefoRiskReportPage.tsx`): tabel risiko + form Kelola Target Holding (owner-only, upsert `product_holding_policy`). **Owner WAJIB mengisi target dulu** supaya laporan ini mulai berguna — belum ada nilai default yang diisi (sengaja, bukan keputusan yang boleh diasumsikan). Diuji 2 skenario simulasi role (rollback: owner lintas-site, lead tidak bocor lintas-site) + smoke-test REST API produksi.
  - **[SELESAI — migration 0033]** (e) ~~Papan Favorit~~: tabel `terima_cepat_favorites` (murni preferensi UI per user via `auth.uid()`, BUKAN data bisnis/inventory — tidak tunduk rule append-only/reversal, boleh UPDATE/DELETE biasa). Klik favorit di `TerimaCepatPage.tsx` mengisi Supplier/Site/Tank/Produk sekaligus (qty/harga tetap manual tiap hari). Diuji 4 skenario simulasi role (rollback) + smoke-test REST API produksi.
  - **[SELESAI — migration 0034]** (b) ~~Rekonsiliasi Kas~~: keputusan desain dikonfirmasi user dulu lewat pertanyaan eksplisit sebelum dibangun (self-reconcile oleh PIC, approve owner MENGUNCI periode dari reversal). Tabel `cash_reconciliations` + `create_cash_reconciliation()` (system_balance/variance dihitung server) + `approve_cash_reconciliation()` (approve otomatis insert baris `adjustment` menyamakan saldo ke fisik) + `create_cash_reversal()` diperbaiki menolak reversal baris yang sudah tercakup rekonsiliasi approved. Halaman `Finance > Rekonsiliasi Kas` (`RekonsiliasiKasPage.tsx`): form pengajuan (lead/staf) + antrian persetujuan (owner). Diuji 7 skenario simulasi role (rollback) + smoke-test REST API produksi.
  - **BELUM dikerjakan (sengaja ditunda)**: (d) Mode Offline/Outbox untuk staf lapangan — perubahan arsitektur besar (service worker, penyimpanan lokal, sinkronisasi, resolusi konflik), user memilih menunda sampai ada kebutuhan nyata dari lapangan (bukan dikerjakan asal tebak sekarang).
- **[SELESAI]** Rekonsiliasi terhadap `Downloads/PRD-MASTER-lobster-sc-os.md` (22 Sept) — dokumen ini eksplisit menyatakan sendiri BUKAN hasil inspeksi langsung ke repo/DB, jadi banyak asumsi skemanya (nama tabel dsb.) beda dari implementasi nyata. Dicocokkan FR demi FR: FR-AUTH-001/FEFO-001/FEFO-002/AUDIT-001/CASH-001/CASH-002/PRICE-001/PNL-001/IMPORT-001/IMPORT-002 SEMUA sudah terpenuhi lewat implementasi nyata (nama beda, intent sama — mis. `max_holding_hours` di PRD jadi `product_holding_policy` per produk+track, lebih baik). **Sengaja TIDAK dibangun (alasan tetap berlaku, sudah didisclosure sebelumnya)**: FR-PRICE-002 (warning spread ke harga jual — butuh acuan harga jual per produk akurat yang skema ini tidak bisa hitung benar sejak delivery multi-produk); tingkat "katalog statis" di `ref_price()` (tidak ada tabel katalog, mengisi kosong = karangan); FR-OFFLINE-001 (Outbox — user pilih tunda sampai ada kebutuhan nyata).
  Empat item yang PRD sendiri tandai `[OPEN QUESTION]` (Bagian 3: "status Risk Register, prs/pos, site_forecast, sop_cycles masih OPEN QUESTION, belum keputusan final") dibahas SATU PER SATU dengan user (bukan blanket yes/no) sebelum eksekusi, sesuai instruksi PRD sendiri Bagian 17 poin 10 ("Tanya klarifikasi... JANGAN tebak dan lanjut coding"):
  - **prs/pos (approval pembelian bertingkat)**: DIJATUHKAN — model beli trading itu cepat & oportunistik (nelayan datang bawa hasil tangkapan, harus dicatat saat itu juga), approval berjenjang sebelum transaksi akan memperlambat justru di titik yang paling butuh kecepatan.
  - **Risk Register**: DIJATUHKAN — alat governance untuk organisasi besar, tim pilot Ernawa belum punya kebutuhan konkret (risiko apa yang mau dicatat, siapa yang review rutin).
  - **sop_cycles**: DIJATUHKAN — budidaya Cimahi belum beroperasi, tidak ada operasi nyata untuk di-track SOP-nya; membangun sebelum tahu detail operasional riil berisiko salah desain.
  - **site_forecast**: **[SELESAI — migration 0035]** DIBANGUN (dikonfirmasi meski model masih kasar) — lihat halaman `Source > Perkiraan Pasokan` (`PerkiraanPasokanPage.tsx`), `get_site_forecast()`: proyeksi SEDERHANA (rata-rata harian dari riwayat penerimaan, diekstrapolasi), SENGAJA bukan model time-series (data pilot masih tipis, model rumit cuma mengarang presisi). `data_points` ditonjolkan di UI biar user menilai sendiri reliabilitasnya. Diuji 3 skenario simulasi role (rollback) + smoke-test REST API produksi.
  Dua Open Question lain dari daftar konsolidasi PRD (Bagian 18) yang genuinely masih terbuka (di luar 4 item di atas) juga ditutup:
  - **[SELESAI — migration 0036]** #5 "Definisi final gm_target_pct 30%... harus konsisten di semua layar": dikonfirmasi margin PER TRANSAKSI (bukan ROI turnover modal), angka HARUS owner-configurable (bukan hardcode 30%, konsisten pola `product_holding_policy`). Tabel `finance_targets` per track, owner-only insert/update. `FinancePage.tsx` dapat KPICard "Margin %" (SUM margin/SUM revenue, bukan rata-rata dari rata-rata) dgn badge target vs aktual + form ubah target owner-only. Diuji 4 skenario simulasi role (rollback).
  - **[SELESAI]** #11 "Format export laporan... belum dikonfirmasi": dikonfirmasi CSV, untuk P&L Finance + Riwayat Penerimaan/Delivery + Piutang & Aging (sebelumnya NOL implementasi export di aplikasi). `src/lib/exportCsv.ts` (utilitas generik, angka mentah tanpa format Indonesia — portable ke spreadsheet app manapun), tombol "Unduh CSV" di 4 halaman, mengekspor baris yang sedang termuat (menghormati filter aktif). Sisa Open Question PRD lainnya (gm_target_pct excluded) sudah moot/terjawab implisit lewat implementasi nyata yang sudah berjalan (stack, `users.site_code`→`user_sites`, `profitability()`→view terpisah per metrik, Finance akses lintas track, approval SO satu langkah, migrasi data v43→fresh start) — tidak butuh tindakan lebih lanjut.
- **[SELESAI]** Rekonsiliasi terhadap DUA dokumen PRD lain yang ditemukan di folder Downloads user (`PRD-Ernawa-SC-OS-v3.docx` dan `PRD-Lobster-Supply-Chain-OS.docx`, keduanya belum pernah dicocokkan — beda dari `PRD-MASTER-lobster-sc-os.md` yang sudah direkonsiliasi sebelumnya), ditemukan lewat audit proaktif (ditanya user "ada lagi selain task itu?", bukan dari permintaan eksplisit menyebut file ini). Isi kedua dokumen di-cross-check terhadap skema/kode nyata (bukan cuma dibaca sekilas) — mayoritas sudah tercakup lewat implementasi yang berbeda nama tapi sama intent. **Dua gap NYATA yang lolos sebelumnya, dikonfirmasi lewat query SQL langsung (bukan tebakan), keduanya diperbaiki**:
  - **[SELESAI — migration 0037]** P10 (v3): "Harga beli dikunci pada saat alokasi, COGS delivery pakai harga saat itu, bukan harga terbaru pemasok." `delivery_allocations` sebelumnya TIDAK punya kolom harga sendiri — `v_trading_delivery_margin` (basis P&L Finance, dashboard investor, KPICard Margin%) live-JOIN ke `receiving_lots.buy_price_per_kg` SAAT QUERY. `receiving_lots_update` RLS memang owner-only, tapi policy itu SENDIRI mengizinkan owner UPDATE harga (mis. koreksi salah input) — kalau itu terjadi setelah ada delivery yang sudah dialokasikan dari batch_line tsb, margin/COGS delivery LAMA yang sudah dilaporkan akan diam-diam berubah retroaktif tiap kali dashboard dibuka lagi. Bertentangan langsung dengan prinsip inti proyek (aturan #2/#3: angka historis tidak boleh berubah diam-diam). Belum pernah ketahuan karena belum ada UI yang mengedit `receiving_lots.buy_price_per_kg` — tapi RLS-nya sudah mengizinkan.
    **Resolusi**: `delivery_allocations.buy_price_per_kg` (NOT NULL, CHECK > 0) menyimpan snapshot harga saat itu juga; `create_delivery_with_allocations()` menangkapnya dari SELECT batch_line yang sudah ada (tidak perlu query tambahan); `v_trading_delivery_margin` (dan `investor_trading_delivery_margin()` yang delegasi ke view ini) pakai `sum(da.qty_kg * da.buy_price_per_kg)` langsung, bukan join ke `receiving_lots` lagi. Diuji dengan proof BEFORE/AFTER eksplisit: alokasi → catat COGS → mutasi harga upstream → konfirmasi COGS lama terbukti TIDAK berubah (bukan cuma happy-path).
  - **[SELESAI — migration 0038]** P11 (v3): "Penyelesaian memakai berat timbang akhir, selisihnya dicatat sebagai baris ledger tersendiri, bukan diserap diam-diam." `fn_delivery_allocations_ledger` mengurangi stok sejumlah qty yang DIALOKASIKAN saat alokasi terjadi; `KonfirmasiTimbangPage.tsx` cuma UPDATE `deliveries.actual_weight_kg` langsung tanpa pernah merekonsiliasi ke ledger — susut selama transit (actual < allocated) sebelumnya tidak tercatat di mana pun. Gap yang sama persis dengan Handover (migration 0029) yang sudah eksplisit menangani pola ini, tapi Delivery-nya luput. Dikonfirmasi ke user lewat pertanyaan eksplisit (catat sebagai shrinkage otomatis vs biarkan) sebelum dibangun — user pilih shrinkage otomatis.
    **Resolusi**: trigger baru `fn_deliveries_confirm_weighing_variance` (SECURITY DEFINER — perlu karena `inventory_ledger` INSERT policy owner-only, sama seperti `fn_handover_lines_confirm_variance`) bereaksi saat `actual_weight_kg` pertama kali diisi (NULL → ada nilai). TIDAK ADA perubahan ke `KonfirmasiTimbangPage.tsx` — masih plain UPDATE seperti sebelumnya, trigger menangani konsekuensi ledgernya otomatis. Kalau delivery punya >1 `delivery_allocations` (multi-batch), selisih didistribusi proporsional sesuai porsi qty_kg tiap baris; kalau actual ≥ allocated (tidak ada susut/kelebihan timbang), tidak ada baris dibuat (tidak mengarang stok dari udara). Diuji 3 skenario simulasi role (rollback): single-line varian oleh non-owner, tanpa varian (no-op), multi-line proporsional (6:4 split dari shortfall 2kg → -1.2kg/-0.8kg). Sengaja TIDAK diduplikasi ke `stock_adjustments` (itu reserved utk penyesuaian manual dgn alasan diketik) — konsisten dgn presedan Handover.
  **BELUM diputuskan/dibangun (baru mengambang dari analisis sendiri, bukan diputuskan bareng user)**: 4 "gap Finance" dari sistem v43 lama yang disebut di `PRD-Lobster-Supply-Chain-OS.docx` (v1) — penanganan PPN/PPh, ledger kas/bank perusahaan level treasury (terpisah dari Kas Panjar per-PIC), breakdown kategori opex, laporan budget-vs-actual. ("Kontribusi Rp/kg" dari daftar yang sama SUDAH tercakup lewat KPICard "Margin per kg" yang ada.) Perlu diangkat ke user sebagai keputusan terpisah kalau ingin penutupan penuh v1 — belum tentu termasuk cakupan instruksi yang memicu rekonsiliasi ini.
- **[SELESAI — migration 0039]** Performance advisor Supabase (`get_advisors(type='performance')`) belum pernah dicek sepanjang sesi ini — baru security advisors yang rutin dipantau. Ditemukan lewat audit proaktif yang sama dengan penemuan dokumen PRD v3/v1 di atas. Finding WARN `auth_rls_initplan` (11 policy: `users_select_self`/`_insert_self`/`_update_self`, `terima_cepat_favorites_select`/`_insert`/`_delete`, `cash_ledger_select`/`_insert`, `cash_reconciliations_select`/`_insert`, `user_sites_select`) — `auth.uid()` (dan `auth_user_role()`/`user_can_access_site()`, sama-sama STABLE lewat `pg_proc.provolatile`) dipanggil TELANJANG di USING/WITH CHECK, bukan dibungkus `(select ...)`, jadi planner Postgres mengevaluasinya ulang untuk SETIAP BARIS yang dipindai alih-alih sekali per-query — signifikan begitu volume baris bertambah (relevan mengingat migration 0033 baru menambah `terima_cepat_favorites` dan 0034 `cash_reconciliations`, keduanya langsung kena).
  **Resolusi**: `DROP POLICY`+`CREATE POLICY` di 11 policy tsb, kondisi USING/WITH CHECK PERSIS SAMA secara semantik, cuma tiap pemanggilan `auth.uid()`/`auth_user_role()`/`user_can_access_site()` dibungkus `(select ...)` supaya planner bisa mengubahnya jadi InitPlan (sekali per-query). MURNI optimisasi planner, bukan perubahan logika akses. Diuji lewat simulasi role di transaksi yang di-rollback SEBELUM diterapkan (disiplin proyek): (1) snapshot hasil SELECT `users`/`cash_ledger`/`user_sites` sbg lead & owner SEBELUM vs SESUDAH policy diganti — 100% identik di keenam kombinasi; (2) kesetaraan boolean WITH CHECK `cash_ledger_insert` & `cash_reconciliations_insert` dievaluasi langsung (tanpa insert sungguhan, menghindari efek samping trigger/guard) untuk 8 kombinasi input yang sengaja mencakup hasil `true` DAN `false` — semua cocok. `get_advisors` dicek ulang setelah diterapkan: lint `auth_rls_initplan` sudah tidak muncul lagi.
  **BELUM ditangani (di luar cakupan instruksi user kali ini, cuma ditemukan bareng)**: performance advisor juga melaporkan `multiple_permissive_policies` WARN (2× di tabel `users`, utk SELECT: `users_select`+`users_select_self`, dan UPDATE: `users_update_by_owner`+`users_update_self` — beda dari `auth_rls_initplan`, perbaikannya butuh KONSOLIDASI dua policy jadi satu, bukan cuma bungkus sintaks, jadi butuh keputusan desain sebelum diubah, bukan cuma dieksekusi diam-diam), serta level INFO (bukan WARN, prioritas lebih rendah): `unindexed_foreign_keys` (35 FK) dan `unused_index` (4 index — "belum pernah dipakai" masih wajar mengingat volume data pilot masih sangat tipis, belum tentu benar-benar tidak diperlukan). Perlu diangkat ke user sebagai keputusan terpisah kalau ingin ditindaklanjuti.
- **[SELESAI — migration 0040]** Gap Finance #2 dari 4 sisa `PRD-Lobster-Supply-Chain-OS.docx` (v1) yang sebelumnya cuma dicatat mengambang: "saldo kas/bank riil" — sistem sekarang (bukan cuma prototipe lama) juga punya gap yang sama: `cash_ledger` yang ada murni Kas Panjar per-PIC/site (uang tunai fisik di tangan lapangan), tidak ada representasi kas/bank level PERUSAHAAN sama sekali (topup ke PIC tidak tercatat sumbernya keluar dari mana, pelunasan piutang di `settlements` cuma menandai delivery "lunas" tanpa mutasi kas tercatat).
  Tiga keputusan desain dikonfirmasi user via pertanyaan eksplisit sebelum dibangun (bukan ditebak): (1) auto-link dari alur yang sudah ada (topup Kas Panjar → kas keluar; settlement lunas → kas masuk) + entri manual Owner utk sisanya (bayar supplier, opex, dll) — bukan murni manual semua; (2) satu saldo gabungan, bukan multi-rekening/akun bank terpisah; (3) akses baca Owner + Investor (read-only), konsisten pola investor_* Finance yang sudah ada (migration 0022) — investor tetap TIDAK pernah SELECT tabel mentah, cuma lewat `investor_company_cash_ledger()`.
  **Resolusi**: tabel `company_cash_ledger` (append-only, `track` wajib trading/budidaya per aturan #1, TANPA guard saldo non-negatif SENGAJA — beda dari Kas Panjar yang uang tunai fisik, buku kas perusahaan ini boleh "kelihatan minus" saat data historis belum lengkap diinput, guard ketat akan memblokir pengisian data lama yang legitimate). Dua trigger SECURITY DEFINER (pola sama seperti semua trigger penulis ledger lain di proyek ini — `inventory_ledger` INSERT policy owner-only jadi trigger WAJIB DEFINER supaya tetap jalan utk caller non-owner): `fn_cash_ledger_to_treasury` (topup Kas Panjar → treasury out; reversal dari topup juga menghasilkan treasury in dgn `reversal_of` dirantai balik ke baris treasury topup asli, bukan baris lepas) dan `fn_settlements_to_treasury` (dua jalur: INSERT langsung dgn `settled_at` terisi [mode cod/lunas seketika], atau transisi `settled_at` NULL→terisi lewat tombol "Tandai Lunas" PiutangPage — pola identik `fn_deliveries_confirm_weighing_variance`/migration 0038, TIDAK ada perubahan ke halaman React manapun). `create_company_cash_reversal()` utk koreksi manual (owner-only, anti-double-reversal, pola identik `create_cash_reversal` Kas Panjar). Halaman `Finance > Kas & Bank Perusahaan` (`KasBankPerusahaanPage.tsx`): KPI saldo per track + total gabungan, form entri manual (Owner-only, dropdown kategori sengaja TIDAK menawarkan `settlement_in`/`kas_panjar_topup_out` supaya tidak dobel-hitung dgn yang auto-link), riwayat + tombol Koreksi.
  Diuji 19 skenario simulasi role di transaksi rollback SEBELUM diterapkan (disiplin proyek): topup→treasury out, reversal topup→treasury in dgn rantai `reversal_of` benar, settlement cod oleh lead langsung memicu treasury in (RLS insert settlements terbuka utk lead, trigger WAJIB definer), settlement term (lead) belum memicu apa pun sampai owner "Tandai Lunas", manual entry owner sukses, manual entry lead ditolak RLS (dgn pesan error, bukan silent fail), SELECT langsung oleh lead = 0 baris, investor lihat via function tapi lead tidak, reversal cuma owner & tidak bisa dobel — semua lulus. `database.ts` di-generate ulang, `tsc -b` & `npm run build` bersih.
  **SISA 3 gap Finance dari daftar v1 yang sama, masih mengambang**: PPN/PPh, breakdown kategori opex, laporan budget-vs-actual (breakdown opex & budget-vs-actual sengaja BELUM digabung ke migration ini meski berdekatan tema, supaya tetap satu keputusan desain per migration — akan dibahas satu per satu kalau/ketika diangkat, sesuai disiplin proyek).
- **[SELESAI — migration 0041]** Gap Finance #4 dari daftar v1 di atas: "laporan varians budget-vs-actual bulanan". Dikonfirmasi user cakupannya Revenue + Opex (bukan cuma salah satu) sebelum dibangun.
  **Resolusi**: tabel `finance_budgets` (track+bulan+line_item, config data biasa spt `finance_targets`/`product_holding_policy` — boleh UPDATE langsung, BUKAN ledger append-only, karena ini angka TARGET yang sengaja diubah-ubah, bukan catatan transaksi yang sudah terjadi). Baca seluas `finance_targets` (owner/lead/staf/investor), tulis owner-only. `get_budget_actuals()` SECURITY DEFINER menghitung realisasi: revenue dari `v_trading_delivery_margin` (join `deliveries.delivered_at` utk pengelompokan bulan; budidaya otomatis selalu 0 krn belum ada operasi budidaya), opex dari `company_cash_ledger` kategori `'opex'` (migration 0040) — WAJIB definer krn `company_cash_ledger` SELECT owner-only (migration 0040) tapi laporan varians ini boleh dilihat role lain juga; cuma mengembalikan angka AGREGAT per panggilan (bukan baris mentah), pola sama `investor_*`/migration 0022. Halaman `Finance > Anggaran vs Realisasi` (`AnggaranRealisasiPage.tsx`): pilih track+bulan, dua KPI (Revenue, Opex) dgn realisasi vs target vs varians Rp & %, form ubah target owner-only.
  Diuji 12 skenario simulasi role di transaksi rollback SEBELUM diterapkan: actual dihitung benar dari fixture settlement (revenue) + company_cash_ledger opex, budidaya & bulan di luar periode = 0 (tidak ikut kehitung), lead broad-read `finance_budgets` sukses + panggil `get_budget_actuals()` sukses, lead insert budget DITOLAK RLS, owner insert & upsert/update budget sukses, investor panggil `get_budget_actuals()` sukses. Semua lulus. `database.ts` di-generate ulang, `tsc -b` & `npm run build` bersih.
  **SISA 2 gap Finance dari daftar v1 yang sama, masih mengambang**: PPN/PPh, breakdown kategori opex — akan dibahas satu per satu kalau/ketika diangkat.
- **[SELESAI — migration 0042]** Gap Finance #3 dari daftar v1 di atas: "breakdown kategori opex" — `company_cash_ledger.category='opex'` (migration 0040) sebelumnya satu bucket datar, tidak bisa dibedakan sewa/gaji/listrik/dll. Dikonfirmasi user lewat pertanyaan eksplisit: (1) kategori dikelola OWNER SENDIRI lewat UI, bukan daftar tetap di-hardcode; (2) `finance_budgets` (migration 0041) TETAP satu angka opex gabungan per bulan — breakdown ini sengaja cuma utk pelaporan/riwayat, TIDAK mengubah skema budget.
  **Resolusi**: tabel `opex_categories` (master data murni, boleh UPDATE nama biasa spt `tanks`/`products` — tidak tunduk rule append-only, baca terbuka `using (true)` krn tidak sensitif). `company_cash_ledger` dapat kolom `opex_category_id` + CHECK constraint (`category <> 'opex' or opex_category_id is not null`) — aman diterapkan langsung krn `company_cash_ledger` masih kosong sama sekali di produksi saat migration ini dibuat. Halaman `Finance > Kelola Kategori Opex` (`KelolaKategoriOpexPage.tsx`, pola identik `KelolaTankPage`/`KelolaProdukPage`). `KasBankPerusahaanPage.tsx` diupdate: dropdown Kategori Opex wajib muncul & wajib diisi saat Jenis='Opex' dipilih di form manual, kolom "Kategori Opex" baru di tabel riwayat, section baru "Opex per Kategori" (breakdown NET per kategori dihitung client-side dari rows yang sudah termuat — otomatis memperhitungkan reversal krn cuma menjumlah `amount` bertanda, bukan cuma baris non-reversal).
  Diuji 6 skenario simulasi role di transaksi rollback SEBELUM diterapkan: owner insert kategori sukses, opex TANPA kategori DITOLAK CHECK constraint, opex DENGAN kategori sukses, kategori manual lain (mis. `supplier_payment`) tetap sukses tanpa wajib isi `opex_category_id`, lead broad-read `opex_categories` sukses, lead insert kategori DITOLAK RLS. Semua lulus. `database.ts` di-generate ulang, `tsc -b` & `npm run build` bersih.
  **SISA 1 gap Finance dari daftar v1 yang sama, masih mengambang**: PPN/PPh.
- **[SELESAI — migration 0043]** Gap Finance #1 terakhir dari daftar v1 di atas: "penanganan pajak PPN/PPh". Dikonfirmasi user lewat pertanyaan eksplisit sebelum dibangun (bukan ditebak, krn salah asumsi status pajak berisiko serius): (1) Ernawa **belum/bukan PKP** → TIDAK ADA kewajiban PPN sama sekali saat ini — sengaja TIDAK membangun apa pun terkait PPN (tidak ada field harga-termasuk-pajak, tidak ada PPN Keluaran/Masukan); kalau status PKP berubah di masa depan, ini butuh migration terpisah dgn keputusan desain baru, bukan diasumsikan dari sekarang. (2) Cukup **MENCATAT** pajak yang sudah dihitung/dibayar (mis. oleh akuntan) — BUKAN menghitung kewajiban pajak otomatis dari transaksi (tidak ada logika PPh Final UMKM 0.5% dari omzet dsb.). (3) Dipecah per JENIS pajak (PPh Final UMKM, PPh 21, PPh 23, Lainnya) — **TETAP/hardcode** (CHECK constraint), BEDA dari `opex_categories` yang dikelola Owner dinamis (migration 0042), karena jenis PPh ditentukan aturan pajak yang berlaku umum, bukan preferensi bisnis Ernawa.
  **Resolusi**: kategori baru `'tax'` di `company_cash_ledger` (pajak yang dibayar itu sendiri memang kas keluar level perusahaan, jadi masuk ledger yang sama, bukan tabel terpisah). Kolom `tax_type` baru (CHECK enum tetap, bukan FK ke tabel terpisah spt `opex_category_id`) wajib diisi kalau `category='tax'`, pola CHECK constraint identik migration 0042. `KasBankPerusahaanPage.tsx` diupdate: dropdown Jenis Pajak wajib muncul & wajib diisi saat Jenis='Pajak' dipilih, kolom gabungan "Kategori Opex / Jenis Pajak" di riwayat, section baru "Pajak per Jenis" (breakdown NET per jenis, pola sama persis "Opex per Kategori").
  Diuji 5 skenario simulasi role di transaksi rollback SEBELUM diterapkan: tax TANPA tax_type DITOLAK, tax DENGAN tax_type valid sukses, tax_type tidak valid DITOLAK, plus REGRESI migration 0042 (opex tanpa `opex_category_id` masih ditolak, opex dgn kategori masih sukses — constraint lama tidak rusak oleh migration ini). Semua lulus. `database.ts` di-generate ulang, `tsc -b` & `npm run build` bersih.
  **Dengan ini SEMUA 4 gap Finance dari `PRD-Lobster-Supply-Chain-OS.docx` (v1) sudah tertutup**: kas/bank riil (0040), budget vs actual (0041), breakdown opex (0042), PPN/PPh (0043).
- **[SELESAI — migration 0044]** Audit "apakah UI sudah sesuai PRD Control Tower" (permintaan user langsung). PRD v3 cuma menyebut "Control Tower" SATU KALI, sbg satu item dalam daftar cakupan Fase D, tanpa spek lanjutan — dokumen v3 sendiri menyatakan perilaku fitur detail (F1–F13) ada di "PRD V2" yang TIDAK ADA di antara 3 dokumen PRD yang ditemukan di Downloads. Ditemukan lewat `Artifact({action:"list"})` bahwa ada artifact lama bernama persis "Lobster Trading Control Tower" (https://claude.ai/artifact/2CA6nY2zPHbzFLm1Lghr4f, prototipe lebih lama dari sistem v43) — dibaca penuh source code-nya sbg acuan konkret pengganti PRD yang tidak ada.
  Dibandingkan tab "Ringkasan" artifact itu terhadap sistem sekarang, ditemukan 5 gap nyata (bukan tebakan — dicek langsung ke skema & kode): CCC+DPO (belum ada perhitungan/field sama sekali), Panel Peringatan Aktif terpadu (alert tersebar di 3 halaman terpisah, tidak ada yang menyatukan), breakdown margin per Grade & Segmen Pelanggan (tidak ada), indikator data trust/verifikasi (konsep tidak ada), dan halaman Pengaturan Ambang terpusat (ambang tersebar, sebagian belum configurable). User pilih prioritas: CCC+DPO, breakdown margin, Panel Peringatan — dikerjakan satu per satu, breakdown margin duluan (lebih sederhana, tidak butuh data baru sebesar CCC+DPO).
  **Breakdown margin per Produk & Segmen** — dua keputusan desain dikonfirmasi user dulu krn ternyata "Grade" DAN "per Produk" sama-sama kena masalah struktural yang sama (revenue per delivery itu satu angka `settlements.amount`, tidak bisa dipecah akurat kalau delivery berisi >1 produk sejak migration 0026 — akar masalah IDENTIK dgn kenapa `ref_price()` tidak bisa hitung harga jual per produk, migration 0028): (1) "Grade" diganti "Produk" (grade kualitas tidak bertaut bersih ke delivery, produk sudah bertaut bersih); (2) delivery campuran DIKECUALIKAN dari breakdown per-produk (bukan diprorata qty — prorata = mengarang asumsi harga, melanggar prinsip inti proyek), ditampilkan terpisah sbg ringkasan "Campuran" yang jujur. Segmen Pelanggan (Restoran/Eksportir/Lainnya, kolom baru `customers.segment`) TIDAK kena masalah ini krn atribut customer, bukan produk.
  **Resolusi**: view `v_trading_margin_by_product` (delivery satu-produk saja), `v_trading_margin_mixed_summary` (ringkasan delivery campuran), `v_trading_margin_by_segment` — semua `security_invoker`, plus 3 fungsi `investor_*` `SECURITY DEFINER` (pola identik migration 0022). `FinancePage.tsx` dapat 2 section breakdown baru; `CustomerPage.tsx` dapat field & kolom Segmen.
  Diuji 9 skenario simulasi role di transaksi rollback SEBELUM diterapkan — SENGAJA pakai RPC `create_receiving_with_batch`/`create_delivery_with_allocations` SUNGGUHAN utk fixture (bukan insert manual) supaya alur ujinya sedekat mungkin dgn alur nyata: delivery satu-produk masuk breakdown dgn angka tepat (revenue/cogs/margin dihitung manual dulu lalu dicocokkan persis), delivery campuran DIKECUALIKAN dari breakdown per-produk & muncul di ringkasan Campuran dgn angka tepat, margin per segmen menggabungkan kedua delivery dgn benar (tidak kena masalah delivery-campuran krn per-customer), investor lihat via function, lead select langsung view sukses. Semua lulus. `database.ts` di-generate ulang, `tsc -b` & `npm run build` bersih.
  **SISA dari 5 gap Control Tower yang dipilih user**: CCC+DPO dan Panel Peringatan Aktif terpadu (dikerjakan menyusul, lihat entri di bawah); indikator data trust dan halaman Pengaturan Ambang terpusat (TIDAK dipilih user saat prioritas ditanya — bukan lupa, sengaja tidak masuk cakupan).
- **[SELESAI]** Panel Peringatan Aktif terpadu (gap #2 dari daftar Control Tower di atas). Cakupan dikonfirmasi user: batch tertahan (`get_fefo_risk_report()`), piutang lewat tempo (`v_settlements_aging`), margin di bawah target (`v_trading_delivery_margin`+`finance_targets`) — mortalitas di atas ambang SENGAJA belum masuk (belum ada konsep threshold mortalitas di sistem sama sekali, butuh keputusan desain terpisah, ditunda).
  **Lokasi & nominal Rp dikonfirmasi user lewat pertanyaan eksplisit** (bukan diasumsikan): tetap di `HomePage.tsx` (bukan halaman Finance terpisah) DENGAN nominal Rupiah ditampilkan penuh — ini OVERRIDE keputusan desain lama `HomePage.tsx` yang sebelumnya PUNYA KOMENTAR EKSPLISIT "sengaja tidak menampilkan nilai uang (piutang/margin): itu tugas halaman Finance". Komentar itu sudah diperbarui di file utk mencatat revisi ini dan alasannya. Tetap per track (tidak melanggar aturan #1).
  **Resolusi**: MURNI FRONTEND, TIDAK ADA migration baru — ketiga sumber data (`get_fefo_risk_report()`, `v_settlements_aging`, `v_trading_delivery_margin`+`finance_targets`) sudah ada & RLS-aman, dipakai persis pola query yang sama dgn `FefoRiskReportPage`/`PiutangPage`/`FinancePage` yang sudah ada (owner/lead/staf select langsung, investor tidak relevan krn `HomePage` redirect investor ke `/finance`). Severity kritis/peringatan murni heuristik tampilan (batch >2x ambang holding, piutang >60 hari lewat tempo, margin gap >5pp) — bukan nilai/kolom baru di DB.
  **SISA dari 5 gap Control Tower**: CCC+DPO (dipilih user, belum dikerjakan); indikator data trust dan halaman Pengaturan Ambang terpusat (di luar cakupan yang dipilih user).
- **[SELESAI — migration 0045]** DPO + Cash Conversion Cycle (gap terakhir dari 5 gap Control Tower). CCC = Inventory Days + DSO − DPO; dua komponen pertama sudah ada (`v_trading_capital_lockup`, `v_trading_receivable_cycle`), DPO belum ada sama sekali — tidak ada field termin bayar ke supplier di skema manapun.
  Dikonfirmasi user lewat pertanyaan eksplisit: (1) termin per SUPPLIER (bukan per transaksi penerimaan), pola identik `customers.payment_term_days`; (2) realitas bisnis dikonfirmasi — sebagian/semua supplier Ernawa memang punya termin bayar (bukan selalu tunai di tempat spt yang disebut PRD v3 Bagian 7 utk Kas Panjar — klarifikasi PENTING: kalimat itu benar utk SEBAGIAN pembelian/supplier, bukan generalisasi semua pembelian selalu tunai), jadi DPO relevan dihitung, bukan selalu 0.
  **Resolusi**: `suppliers.payment_term_days` baru. View `v_trading_dpo_inputs` — granularitas SAMA PERSIS dgn `v_trading_capital_lockup` (per `delivery_allocation`, trading, delivered, tidak batal) supaya CCC konsisten populasinya; `buy_price_per_kg` dari `delivery_allocations` (harga terkunci, migration 0037). Receiving asal Serah Terima (handover, `supplier_id` NULL) DIKECUALIKAN — bukan pembelian riil dari supplier eksternal, konsisten pola `ref_price()` migration 0028 yang mengecualikan hal sama. `payment_term_days` NULL (supplier belum diklasifikasi) TETAP muncul di view — exclusion dari rata-rata tertimbang DPO itu tanggung jawab FRONTEND (dihitung hanya dari baris yang terisi, BUKAN diasumsikan 0 = mengarang data), dgn coverage % (nilai pembelian yang termin-nya terisi / total) ditampilkan biar user tahu seberapa representatif angkanya — konsisten prinsip "jangan mengisi kosong dgn karangan" (sama alasan `ref_price()` tidak bikin katalog statis). `investor_trading_dpo_inputs()` DEFINER wrapper (pola migration 0022).
  `SourcePage.tsx` (Supplier Management) dapat field & kolom Termin Bayar. `FinancePage.tsx` dapat KPICard DPO & Cash Conversion Cycle (CCC null kalau belum ada riwayat trading sama sekali/`avgLockupDays` null; DSO/DPO yang kosong diperlakukan 0 dalam formula CCC — beda dari "belum ada transaksi sama sekali").
  Diuji 7 skenario simulasi role di transaksi rollback SEBELUM diterapkan (pakai RPC `create_receiving_with_batch`/`create_delivery_with_allocations` SUNGGUHAN utk fixture): baris DPO baru muncul dgn `payment_term_days` & `purchase_value` tepat, receiving_transaction asal handover NYATA yang sudah ada di produksi (dari uji Handover sebelumnya) terkonfirmasi `supplier_id` NULL (jadi benar-benar terkecuali oleh filter — bukan diasumsikan), investor lihat via function, lead select langsung sukses, `payment_term_days` NULL tetap muncul di view (bukan hilang). Semua lulus. `database.ts` di-generate ulang, `tsc -b` & `npm run build` bersih.
  **Dengan ini SEMUA 5 gap Control Tower yang dipilih user (breakdown margin, Panel Peringatan Aktif, DPO+CCC) sudah tertutup.** SISA di luar cakupan yang dipilih: indikator data trust/verifikasi, halaman Pengaturan Ambang terpusat, ambang mortalitas configurable (semuanya sengaja tidak diangkat, bukan lupa).
- **[SELESAI — migration 0046]** Dua item sisa dari audit Control Tower di atas ("indikator data trust/verifikasi" dan "ambang mortalitas configurable") diangkat kembali dan dikonfirmasi user secara eksplisit sebelum dibangun (kedua konsep tidak ada padanan sama sekali di skema sebelumnya).
  **Data Trust**: pola sama persis artifact lama — atestasi MANUAL saat entri ("sudah diverifikasi, cocok bukti transfer/timbang/invoice"), bukan proses verifikasi otomatis. Kolom `verified` baru di `receiving_lots` (setara "batch" di artifact lama) dan `settlements` (setara "sale") — CUMA bisa diisi saat insert, TIDAK ADA UI edit belakangan (sengaja, di luar cakupan). `create_receiving_with_batch()` diupdate menangkap field `verified` opsional per lot (`coalesce(...,false)`, backward-compatible dgn caller lama termasuk Import Excel yang tidak mengirim field ini). View `v_receiving_trust` & `v_settlement_trust` DIBUAT TERPISAH per sisi (BUKAN digabung jadi satu skor tunggal spt artifact lama yang mencampur nilai penerimaan & penjualan) — lebih actionable, konsisten prinsip "jangan menggabungkan angka yang beda maknanya". `investor_receiving_trust()`/`investor_settlement_trust()` DEFINER wrapper (pola migration 0022). `TerimaCepatPage.tsx`/`SettlementPage.tsx` dapat checkbox verified; `FinancePage.tsx` dapat section "Data Trust" (% terpisah Penerimaan vs Settlement, track trading).
  **Ambang Mortalitas**: dikonfirmasi user basis PER SITE, % dari qty diterima dalam periode berjalan. Periode HARDCODE 30 hari (bukan configurable — sengaja, hindari menambah satu lagi knob pengaturan yang belum tentu perlu diubah-ubah; bisa diangkat jadi parameter kalau nanti memang dibutuhkan). Tabel `mortality_thresholds` (pola RLS identik `product_holding_policy`: broad-read owner/lead/staf, tulis owner-only) + `get_mortality_rates(p_days)` `SECURITY INVOKER` (pola identik `get_fefo_risk_report()` — RLS `inventory_ledger`/`batch_lines` yang sudah ada otomatis membatasi per-site). `StokMortalitasPage.tsx` dapat panel tingkat mortalitas 30 hari + form ambang owner-only per site yang sedang dipilih. **Terintegrasi ke Panel Peringatan Aktif** (`HomePage.tsx`) sbg jenis alert ke-4 — menutup catatan yang sebelumnya bilang "mortalitas di atas ambang SENGAJA belum masuk (belum ada konsep threshold)".
  Diuji 17 skenario simulasi role di transaksi rollback SEBELUM diterapkan (pakai RPC `create_receiving_with_batch`/`create_delivery_with_allocations` SUNGGUHAN utk fixture; **site terisolasi khusus** dibuat utk uji mortalitas supaya `received_kg`/`mortality_kg`/`mortality_pct` bisa dicocokkan presisi tanpa kontaminasi data produksi nyata yang ikut ter-agregasi view/fungsi): verified true/false/default(tanpa field) tersimpan benar, delta `v_receiving_trust`/`v_settlement_trust` tepat, `mortality_pct`/`is_overdue` tepat sampai ke angka, investor & lead akses sesuai RLS — termasuk temuan awal yang SEMPAT terlihat seperti gagal (`get_mortality_rates` mengembalikan 0 baris utk lead di site test yang belum ditugaskan) ternyata dikonfirmasi ULANG sbg perilaku RLS site-scoping yang BENAR (migration 0016/0017, gagal-tertutup), bukan bug — diverifikasi dgn query tambahan bahwa lead tetap bisa lihat site yang MEMANG ditugaskan (Banggai). Semua lulus. `database.ts` di-generate ulang, `tsc -b` & `npm run build` bersih.
  **Dengan ini SEMUA gap dari audit Control Tower (baik yang dipilih user di awal maupun dua item yang diangkat kembali belakangan) sudah tertutup.** Yang masih di luar cakupan (tidak pernah diminta): halaman "Pengaturan Ambang" TERPUSAT dalam satu layar tunggal (ambang holding, ambang mortalitas, dan config Finance lain tetap di halaman masing-masing yang sudah ada — `FefoRiskReportPage.tsx`, `StokMortalitasPage.tsx`, `AnggaranRealisasiPage.tsx` — bukan disatukan; kalau user memang menginginkan satu layar gabungan, itu keputusan terpisah yang belum diambil).
- **[SELESAI — migration 0047]** User meminta verifikasi ulang eksplisit "apakah semua sudah sesuai artifact Control Tower" — dibaca ulang FULL artifact (bukan cuma tab Ringkasan seperti audit pertama, tapi juga tab Pengaturan) dan ditemukan 3 gap AR yang luput dari audit sebelumnya: (1) KPI "Piutang Belum Tertagih" (total AR outstanding) sbg satu angka di FinancePage — belum ada; (2) AR Aging 4-bucket (Belum jatuh tempo/1-30/31-60/>60 hari) sbg ringkasan — `PiutangPage` cuma worklist per baris, tidak ada total per bucket; (3) `ar_watch_days` (ambang piutang lewat tempo yang configurable) — Panel Peringatan Aktif (migration terdahulu) KELIRU selalu mengalert piutang manapun yang lewat tempo (>0 hari) tanpa ambang nyata, beda dari pola FEFO/mortalitas yang mensyaratkan Owner mengisi ambang dulu. Dikonfirmasi user utk ditindaklanjuti sekalian.
  **Resolusi**: `v_settlements_aging` (migration 0009) ditambah kolom `track` (join `deliveries`->`sites`, `security_invoker` DIPERTAHANKAN eksplisit lewat `create or replace view ... with (security_invoker=true)` — diuji langsung via `pg_class.reloptions` TERBUKTI tidak regresi ke `security_definer` default, krn CREATE OR REPLACE VIEW tanpa opsi eksplisit bisa diam-diam reset opsi lama) supaya bisa difilter trading-only (aturan #1) tanpa mengganggu `PiutangPage`/`investor_settlements_aging()` yang sudah pakai view ini (kolom baru ditambah di akhir, additive). `finance_targets` dapat kolom `ar_watch_days` — NULLABLE tanpa nilai default (konsisten pola `product_holding_policy`/`mortality_thresholds`: Owner wajib isi dulu sebelum alert jenis ini aktif, BUKAN diam-diam pakai default 30 hari dari artifact lama).
  `FinancePage.tsx` dapat section "Piutang Belum Tertagih" (total + 4 KPI bucket + form ambang owner-only, berbagi baris `finance_targets` yang sama dgn target margin — kalau margin target belum diisi, form ambang AR menolak dgn pesan jelas krn `margin_target_pct` NOT NULL). `HomePage.tsx` Panel Peringatan Aktif: alert piutang sekarang DIGATE oleh `ar_watch_days` (tidak ada alert AR sama sekali sampai Owner mengisi ambang — perbaikan perilaku dari migration sebelumnya yang keliru).
  Diuji 6 skenario simulasi role di transaksi rollback SEBELUM diterapkan: `security_invoker` terbukti tetap `true` setelah `CREATE OR REPLACE VIEW`, kolom `track`/`days_until_due` akurat, `ar_watch_days` tersimpan & broad-read lead sukses, `investor_settlements_aging()` tetap jalan tanpa error meski kolom view bertambah (return type `setof` view otomatis ikut, tidak perlu ubah fungsi). Semua lulus. `database.ts` di-generate ulang, `tsc -b` & `npm run build` bersih.
  **Pelajaran proses**: audit "apakah sudah sesuai spek" pertama kali (migration 0044) cuma menyisir SATU tab dari sumber acuan (Ringkasan) meski sumber acuan itu py 6 tab — verifikasi ulang yang diminta user belakangan (bukan asumsi "sudah pasti lengkap") yang justru menemukan gap nyata di tab lain (Pengaturan). Kalau ada audit serupa di masa depan thd sumber acuan multi-bagian, sisir SEMUA bagian dari awal, jangan berhenti begitu satu bagian utama sudah cocok.
