import { ComingSoonPage } from '../../components/shared/ComingSoonPage';

// Ditampilkan saat tab "Inventory" dibuka tapi belum memilih submenu
// (mis. "Terima Cepat") di sidebar.
export function InventoryIndexPage() {
  return <ComingSoonPage title="Inventory" />;
}
